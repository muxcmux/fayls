server:
  address: 127.0.0.1
  port: 8080
app:
  database: data/fayls.db
  pdftoppm_bin: pdftoppm
  tesseract_bin: tesseract
  batch_size: 1000
  max_concurrent_batches: 10
  max_concurrent_indexes: 10000
